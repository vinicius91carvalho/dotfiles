# build output from `darwin-rebuild build` / `nix build`
result
result-*

.DS_Store

# herdr left this behind: runtime logs, sockets and session state from a
# tool this config no longer declares (Orca replaced it). Never repo content.
.config/herdr/
